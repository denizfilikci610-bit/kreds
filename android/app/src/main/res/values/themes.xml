<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.VibeFeed" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Vises inden WebViewet har tegnet noget. Uden den blinker skærmen hvid. -->
        <item name="android:windowBackground">@color/vf_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
