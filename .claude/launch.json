{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "vibefeed-static",
      "runtimeExecutable": "python3",
      "runtimeArgs": [
        "-c",
        "import http.server, os\nclass H(http.server.SimpleHTTPRequestHandler):\n    def end_headers(self):\n        self.send_header('Cache-Control', 'no-store')\n        super().end_headers()\nhttp.server.ThreadingHTTPServer(('', int(os.environ.get('PORT', 8744))), H).serve_forever()"
      ],
      "port": 8744,
      "autoPort": true
    }
  ]
}
